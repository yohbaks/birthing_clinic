# BirthCare Clinic Management System — Python Dependencies

# Core framework
Django>=5.2,<6.0

# Production WSGI server
gunicorn>=21.0

# Local dev: load .env file
python-dotenv>=1.0

# Image handling (staff profile photos, media uploads)
Pillow>=10.0

# PDF generation (patient summaries, delivery certificates, SOA/receipts)
reportlab>=4.0

# Optional: PostgreSQL support for production
# psycopg2-binary>=2.9

# Optional: WeasyPrint for HTML-to-PDF (alternative PDF backend)
# WeasyPrint>=60.0
